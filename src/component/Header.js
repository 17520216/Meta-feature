import React from 'react'
import PropTypes from 'prop-types'

const Header = props => {
    return (
        <header className="header">
            Header goes Here
        </header>
    )
}

Header.propTypes = {}

export default Header